# 프로젝트: Elsewhen 랜딩페이지

## 서비스 개요
Elsewhen(엘스웬)은 독자의 선택에 따라 실제로 다른 이야기가 되는 웹소설 플랫폼이다.
AI 제작 파이프라인으로 한 작품에 200개 노드, 10개 이상의 결말을 생산한다.
이름은 else + when의 합성으로, "다른 시간, 다른 선택"을 뜻한다.

## 핵심 메시지
- 문제: 기존 인터랙티브 소설은 선택지를 준 척하고 결국 하나의 결말로 합쳐진다
- 해결: AI 제작 파이프라인으로 진짜 갈라지는 분기를 만든다
- 기대효과: 재독 가치, 선택지 무과금

## 타깃
- 1차: itch.io / Steam의 인터랙티브 픽션 독자 (해외)
- 2차: 국내 웹소설 헤비 리더 (회귀·빙의 장르)

## 기술 제약 (반드시 지킬 것)
- HTML, CSS, JavaScript만 사용. React, Vue, 빌드 도구 금지
- 외부 라이브러리 금지 (웹폰트 CDN만 예외)
- index.html은 프로젝트 최상위 경로에 위치
- 폴더 구조: index.html / css/style.css / js/main.js / images/

## 필수 요구사항
- 시맨틱 태그 사용: header, nav, main, section, article, footer
- 필수 섹션: 히어로 / 문제제기 / 기능 소개 / CTA
- 레이아웃은 Flexbox 또는 CSS Grid로 구성
- 미디어 쿼리로 모바일(768px 이하) / 데스크탑 반응형 대응
- JS 인터랙션: 스크롤 등장 애니메이션, 호버 효과, CTA 클릭 시 모달
- 접근성: 모든 img에 alt, 키보드 Tab 탐색 가능, focus 스타일 명시
- 이미지는 외부 URL 대신 SVG로 직접 생성하거나 CSS로 표현

## 디자인 방향
- 배경: #0F0E17 (다크)
- 강조색: #6366F1
- 텍스트: #FFFFFE / 보조 #A7A9BE
- 폰트: Pretendard (CDN 허용)
- 톤: 웹소설과 게임 사이의 감성. 기업형으로 딱딱하지 않게
- radius, 여백 스케일을 CSS 변수로 정의해 일관성 유지

## 디자인 참고
reference/ 폴더의 PlotMate 스크린샷을 참고한다.
카드 radius, 여백 스케일, 버튼 스타일, 강조색(#6366F1)의 감각을 계승하되
배경은 다크(#0F0E17)로 전환한다.

## 작업 규칙
- 파일을 수정하기 전에 무엇을 바꿀지 먼저 설명할 것
- 한 번에 한 섹션씩 작업할 것
- 코드에 한글 주석을 달 것
